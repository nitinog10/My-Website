import ScrollyCanvas from "@/components/ScrollyCanvas";

const Entry = () => {
  return (
    <main className="bg-[#0a0a0b] min-h-screen font-sans">
      <ScrollyCanvas />
    </main>
  );
};

export default Entry;
